package mapping.section01.entity;
// enum : Enumerated type
public enum MemberRole {

    // Enum 타입의 필드는 전부다 상수이다

    /*필기.
    *  Enum 타입을 사용하게 된다면 코드의 가독성 향상
    *  tupe safety(타입의 안정성)을 보장할 수 있다.
    *  서로 연관된 상수들의 집합 클래스이다.*/

    ROLE_MEMBER,
    ROLE_ADMIN
}
